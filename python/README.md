# CellGen Python package

Python parser, validator, serialiser, SVG renderer and annotated-file importer for the [CellGen format](https://github.com/cgps-group/cell-format).

```bash
python -m pip install ".[genbank]"
cellgen validate '()chromosome,{}pKPC[type="plasmid"]'
```
